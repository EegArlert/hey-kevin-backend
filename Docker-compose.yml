services: 
  image_processing:
    image: ${DOCKER_IMAGE_NAME}
    # volumes:
    #   - ${BIND_MOUNT_PATH}:/app/images
    ports:
      - "8000:8000"
    container_name: kevins-segmentation
    environment:
      - IMAGE_URL=${IMAGE_URL}
    env_file:
      - .env    